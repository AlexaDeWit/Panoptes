# Dependencies and build output
node_modules/
dist/
out/
out-tsc/
tmp/
.agents/worktrees/
coverage/

# Test artifacts (vitest coverage, playwright reports and traces)
test-output/

# nx caches and state
.nx/cache
.nx/workspace-data
.nx/polygraph
.nx/self-healing
.nx/migrate-runs

# The default out-link of `nix build`, which writes the rasterizer module
/result

# Toolchain caches
*.tsbuildinfo
.direnv/

# Scan output from the CI semgrep invocation, when run locally
semgrep.sarif

# Editor and OS noise
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
.DS_Store

vitest.config.*.timestamp*
vite.config.*.timestamp*
