{
  "name": "@saerskriven/studio",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "//": [
    "build repeats the Vite plugin's inferred inputs because a leaf override replaces them, then adds the three environment inputs below.",
    "The studio emits external fonts and Pages metadata, so changes to their paths must invalidate the cached build."
  ],
  "dependencies": {
    "@radix-ui/react-icons": "catalog:",
    "@saerskriven/canvas": "workspace:*",
    "@saerskriven/formats": "workspace:*",
    "@saerskriven/model": "workspace:*",
    "@saerskriven/render": "workspace:*",
    "@xyflow/react": "catalog:",
    "effect": "catalog:",
    "radix-ui": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "zod": "catalog:",
    "zustand": "catalog:"
  },
  "devDependencies": {
    "@testing-library/dom": "catalog:",
    "@testing-library/react": "catalog:",
    "@testing-library/user-event": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "ajv": "catalog:"
  },
  "nx": {
    "tags": [
      "layer:app"
    ],
    "targets": {
      "build": {
        "inputs": [
          "production",
          "^production",
          {
            "env": "SAERSKRIVEN_FONTS_DIR"
          },
          {
            "env": "PAGES_BASE_PATH"
          },
          {
            "env": "PAGES_SITE_URL"
          },
          {
            "externalDependencies": [
              "vite",
              "@vitejs/plugin-react"
            ]
          },
          "viteGlobals"
        ]
      },
      "boundaries": {},
      "lint": {},
      "test": {
        "inputs": [
          "...",
          "viteGlobals",
          "studioTestFixtures",
          {
            "env": "SAERSKRIVEN_FONTS_DIR"
          },
          {
            "env": "PAGES_BASE_PATH"
          },
          {
            "env": "PAGES_SITE_URL"
          }
        ]
      }
    }
  }
}
