{
  "name": "@saerskriven/canvas",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./tokens": {
      "types": "./src/lib/tokens.ts",
      "import": "./src/lib/tokens.ts",
      "default": "./src/lib/tokens.ts"
    },
    "./package.json": "./package.json"
  },
  "dependencies": {
    "@saerskriven/model": "workspace:*",
    "@xyflow/react": "catalog:",
    "react": "catalog:"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "react-dom": "catalog:"
  },
  "//": [
    "test deviates from the root dependsOn (CODING.md, Build targets) to name @saerskriven/formats' suite: canvas.fixtures.ts reads test-data/saerskriven.model.json at module load and that suite is what writes it. The layer matrix forbids a package dependency on formats, so ^test cannot reach it, and a dependsOn entry naming the task orders the two with no import. The root's own entries are repeated because a leaf dependsOn replaces the default rather than adding to it."
  ],
  "nx": {
    "tags": [
      "layer:canvas"
    ],
    "targets": {
      "boundaries": {},
      "lint": {},
      "test": {
        "dependsOn": [
          "^build",
          "^test",
          {
            "projects": [
              "@saerskriven/formats"
            ],
            "target": "test"
          }
        ]
      }
    }
  }
}
